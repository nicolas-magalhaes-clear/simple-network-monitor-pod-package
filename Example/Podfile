use_frameworks!

platform :ios, '18.0'

target 'SimpleNetworkMonitor_Example' do
  pod 'SimpleNetworkMonitor', :path => '../'

  target 'SimpleNetworkMonitor_Tests' do
    inherit! :search_paths

    
  end
end
